{
  "name": "kontent-find-duplicates-url-slug",
  "version": "1.0.0",
  "description": "Custom app to find duplicate url-slugs in Kontent.ai pages.",
  "author": "Fabian Payan",
  "license": "MIT",
  "scripts": {
    "dev": "vite --host localhost --mode development",
    "dev:secure": "vite --host localhost --mode development",
    "dev:tunnel": "echo '🚀 Starting development server...' && npm run dev:secure & sleep 3 && npx ngrok http https://localhost:5176 --log stdout",
    "dev:netlify": "netlify dev",
    "dev:netlify:live": "netlify dev --live",
    "build": "tsc -b && vite build --mode production",
    "build:dev": "tsc -b && vite build --mode development",
    "preview": "vite preview",
    "format": "biome check .",
    "format:fix": "biome check --write ."
  },
  "dependencies": {
    "@kontent-ai/custom-app-sdk": "^1.0.0",
    "@kontent-ai/management-sdk": "^7.7.0",
    "@kontent-ai/stylekit": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "~5.8.2",
    "vite": "^7.1.7",
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.14.0"
  }
}
